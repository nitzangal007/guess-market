# Guess Market Implementation Walkthrough

## Status

The seven-stage plan was approved on 2026-08-15. Stages 1 through 3 are accepted and merged. Stage 4 Tasks 9 and 10 implement and verify the Engine-private saved-state boundary plus complete public Engine orchestration. Stage 4 is ready for Nitzan's review but is not yet accepted or merged. D-072's Clean Sections presentation is approved, and Stage 5 is authorized in this isolated worktree from the verified Stage 4 candidate.

## Purpose

This file is the learning-oriented companion to `docs/planning/IMPLEMENTATION-PLAN.md`. The plan says what to build and in what order. This walkthrough records what was actually built, why each meaningful method exists, what state it reads or changes, how failure behaves, and which test proves it.

An entry changes from `Planned` to `Implemented` only after the focused test passes. An entire stage changes to `Verified` only after its regression gate passes. If implementation differs from the planned signature for a non-material reason, record the actual signature and reason. If it would change an approved design contract, stop and obtain Nitzan's approval before changing the code or this guide.

## How to update this guide

For every implementation task:

1. Add or update the affected method rows.
2. Explain one important Java or design concept introduced by the task.
3. Record the focused test class and test method names.
4. Record the regression command and result.
5. Record the commit after the stage is accepted.
6. Keep verified behavior, inference, and open questions separate.

## Architecture in one view

```text
ConsoleMain
  -> GuessMarketConsoleApp
      -> GuessMarketEngine interface
          -> GuessMarketEngineImpl
              -> MarketEvent and owned domain state
              -> LmsrCalculator
              -> XmlMarketLoader -> JaxbMarketMapper -> generated JAXB types
              -> SavedStateStore -> SavedStateValidator

Engine and UI
  -> immutable guessmarket.dto values
```

The UI translates temporary displayed positions into real event IDs. Engine receives and validates only the real IDs. XML and saved-state candidates remain quarantined until complete validation, then replace the Engine map through one reference assignment.

## Stage 1 test foundation

The repository vendors JUnit Platform Console Standalone 6.1.1 only as a development tool. A standalone runner fits this repository because the approved build uses direct Java 25 compiler and launcher commands instead of Maven or Gradle. The runner never becomes part of a production module or runtime artifact.

The exact JAR is `2,996,922` bytes with SHA-256 `7B16416E5727C645105C31B533440397F20DF68F6AE850C6BFD0CE1D88DB66C3`. Oracle JDK 25.0.4 reports automatic module `org.junit.platform.console.standalone@6.1.1`. Matching license and notice material was copied from the JAR itself into `tools/testing`.

## Stage 1 immutable DTO boundary

DTO means data transfer object. These classes are immutable snapshots that cross module boundaries. They are not the live domain objects that will later own quantities, account state, history, or lifecycle transitions.

Each constructor validates that its snapshot is internally meaningful. `List.copyOf` performs a defensive copy and returns an unmodifiable list, so changing the caller's original list cannot change the DTO. Empty XML-provided text and duplicate labels remain valid, while null nested values, invalid option numbering, invalid quantities, non-finite financial values, and inconsistent winner state are rejected.

Receipt and history totals compare `totalPaid` with `baseShareCost + purchaseCommission` using an eight-ULP binary64 tolerance. ULP means one representable floating-point step at the current scale. This accepts harmless representation differences such as `0.1 + 0.2` compared with `0.3`, including at tiny LMSR scales, while still rejecting materially inconsistent totals.

`MarketEventDetails` uses `OptionalInt` for the winner because an open event has no winner and option zero is never valid. `PurchaseReceipt` retains the exact `MarketEventDetails` instance created by the same successful operation, so the UI will not need a second Engine query that could observe a later state.

## Stage 1 supported Engine contract

`GuessMarketEngine` defines the seven capabilities available to every caller. The future console will store this interface type instead of the concrete implementation type. That makes the capability boundary clear and prevents UI code from depending on domain, XML, persistence, or calculation helpers.

Every expected operation failure crosses the boundary as one checked `EngineOperationException`. `EngineErrorCode` identifies the category, while typed optional getters expose available path, event, option, quantity, status, field, and XML location context. The low-level cause remains available for developer diagnosis.

The UI must not parse `getMessage()`. It will switch on `getCode()` and read typed context, because message wording is presentation detail while the enum and getters are the stable recovery contract. The path context is transient because this exception is not part of the approved saved-state graph; the public getter remains unchanged during normal use.

## Stage 2 cancellation-safe LMSR mathematics

`LmsrCalculator` is a final, stateless, package-private Engine helper. Its four package-private static methods accept only primitive quantities and `b`, return unrounded `double` values, and do not import or mutate DTO, domain, XML, persistence, commission, or console state.

`totalCost` uses log-sum-exp. It divides both quantities by `b`, subtracts their maximum before exponentiation, and then restores that maximum after taking the logarithm. The largest adjusted exponential is therefore `exp(0)`, so large valid quantities do not overflow merely because the direct formula would evaluate `exp(large value)`.

`priceForOption` uses the same subtract-the-maximum softmax idea as a stable two-option logistic calculation. It forms the quantity difference as `long` before converting to `double`. The favored side uses `1 / (1 + exp(-difference / b))`; the disfavored side uses the algebraically equal branch `exp(difference / b) / (1 + exp(difference / b))`. At extreme imbalance an observational price may legitimately become exactly `0.0` or `1.0` in binary64.

`purchaseCost` is deliberately separate from both `totalCost` and display price. Subtracting two independently rounded total costs can cancel a real small delta to zero, while multiplying an already underflowed price by an overflowing exponential can produce an invalid result. The direct delta instead calculates `logP`, `logExpm1`, and their sum `t` in the log domain. `softplus` uses `max(x, 0) + log1p(exp(-abs(x)))`, and the large-`h` branch rewrites `log(expm1(h))` as `h + log1p(-exp(-h))` before `expm1` can overflow.

The whole-branch review and residual re-review exposed two cancellation boundaries when `logP` and the leading `h` term have opposite signs. For every negative quantity difference the calculator uses the exact identity `t = (difference + purchaseQuantity) / b - softplus(z) + log(1 - exp(-h))`. It forms `difference + purchaseQuantity` in `long` before converting once to `double`, preserving low-order numerator information that separately rounded quotients can erase. The correction `log(1 - exp(-h))` uses `log(-expm1(-h))` for `h <= ln(2)`, where subtracting `exp(-h)` from one is vulnerable to cancellation, and `log1p(-exp(-h))` for `h > ln(2)`, where `exp(-h) <= 0.5`. This standard `ln(2)` split keeps both forms in their stable regions. The reachable cases `(qs, qo, d, b) = (0, Integer.MAX_VALUE, Integer.MAX_VALUE - 2, 3)` and `(0, Integer.MAX_VALUE, Integer.MAX_VALUE - 2, 65_075_262)` exercise the large-`h` and moderate-`h` boundaries.

For `t < -37.0`, the calculator evaluates `exp(log(b) + t)`. Keeping the multiplication by `b` inside the logarithm preserves representable tiny costs such as `6.392138950083687E-44`, even when `softplus(t)` alone would be too small. If a mathematically positive purchase still rounds to zero, the method rejects it instead of granting free shares. Nonnegative current quantities, positive purchases, positive `b`, and finite outputs are checked before a future domain operation can mutate state.

`maximumSubsidy` derives `b * ln(2)`. It is a maximum-exposure measure, not cash placed into the market-maker account. The calculator tests cover the approved worked example, supplied-simulator oracle cases, ordinary invariants, larger `b`, extreme quantities, both underflow outcomes, and invalid inputs with documented absolute or relative tolerances.

## Stage 2 event domain and atomic accounting

`MarketEvent` is the aggregate owner. Its public constructor exists only so the later `JaxbMarketMapper` in the sibling `guessmarket.engine.xml` package can construct a validated event. The class exposes no other public method. Its package-private transition, conversion, and observation methods remain internal to Engine callers and same-package persistence validation. Identity-preserving observations return the actual owned nodes and collections only at this trusted package boundary so restore validation can detect accidental mutable aliases.

One event owns two identity-distinct `MarketOption` objects in fixed one-based order, one `CommissionPolicy`, one `EventAccount`, and one chronological `ArrayList<TradeRecord>`. It also owns the open or closed lifecycle and optional winner. The constructor accepts every full-range event ID, empty text, and duplicate option labels, while rejecting null required values, commission outside 0 through 90, and nonpositive `b`. New events start open with zero quantities, zero account balance, zero collected commission, no winner, and empty history.

`purchase` follows validate, calculate, then commit. Before changing any field it proves the event is open, the option and quantity are valid, and checked integer addition cannot overflow. It then asks only `LmsrCalculator` for the base cost and both candidate prices. It calculates the applicable purchase commission, total paid, candidate account balance, candidate commission total, and complete immutable `TradeRecord`, and verifies every required financial value. Only after the full candidate succeeds does it commit the selected quantity, both account values, and the new chronological history record. Arithmetic and representability failures become `FINANCIAL_CALCULATION_FAILED`; all rejected transitions serialize to exactly the same bytes before and after the call in `MarketEventTest`.

Expected failures retain stable error codes while their detail identifies the operation and failed value. A purchase on a closed event and a repeated close both use `EVENT_NOT_OPEN`, but their detail distinguishes purchasing shares from closing the event. Numerical failures identify values such as base share cost or purchase commission without exposing UI wording or low-level implementation text. The positive 1 percent commission-underflow case at `b = 1` after 742 favored shares proves the checked failure preserves serialized values and every owned identity.

`close` uses the same transaction shape. It validates the lifecycle and winner, then calculates gross payout, applicable on-close commission, net payout, candidate balance, and candidate commission total before mutation. Its single commit section updates the account once, records the winner, and changes the status to `CLOSED`. Subtracting only net payout retains an on-close commission, so the balance never receives a second commission credit. The final signed balance is preserved: positive is profit, negative is subsidy, and exact zero is break-even.

Internal history stays chronological for accounting and restore validation. `toDetails` creates new immutable DTO values in reverse chronological order for newest-first display. It derives both current prices on demand, so neither price nor `b * ln(2)` enters the serialized graph. `toSummary`, `toDetails`, and `maximumSubsidy` are observations only and cannot mutate the aggregate.

The five Task 5 domain classes implement `Serializable` and each declares `private static final long serialVersionUID = 1L`. The two DTO enums retain normal enum serialization identity. The owned collection fields use the concrete serializable `ArrayList` type, which also satisfies Java 25 serial lint. DTO classes, `LmsrCalculator`, Engine coordination types, XML types, and technical file objects were not added to the reachable event graph.

## Stage 3 Task 6: XML resources and retained JAXB source

Task 6 establishes the reproducible XML inputs before mapper or loader behavior exists. The production Engine resource owns a byte-identical trusted copy of `GM-EX1-Schema.xsd`; four course fixtures live under `fixtures/supplied` with their canonical SHA-256 values preserved. Four separately authored D-069 fixtures live under `fixtures/custom`, including the full `xs:int` range, duplicate nonpositive IDs, present empty text and duplicate labels, a valid-schema one-option business failure, commission `0` and `90`, positive `b`, a filename containing spaces, and a missing-required-description schema failure.

The intact JAXB RI 4.0.5 wrapper generated eight retained types in `guessmarket.engine.xml.generated` from the verified schema: `Comision`, `GMEvent`, `GMEvents`, `GMLMSR`, `GMMethod`, `GMOptions`, `GuessMarket`, and `ObjectFactory`. Every generated source declares the approved package. During Task 6, each retained file matched its independently generated staging counterpart. The durable retained-file hashes are:

| Retained generated source | SHA-256 |
| --- | --- |
| `Comision.java` | `DCC3014FDD35D2D99C48CC2A7FB2B39169403156C6C6542AA513D0DE3BAC50E6` |
| `GMEvent.java` | `B85D47A67CE08BCF2D5121E799D112BEA7389987BB90FF739D61381363B7AE11` |
| `GMEvents.java` | `696B57E5C2A2AB0354D02A077B5F1EECED02DDA0A04C2FA84768C6B73B68BDA2` |
| `GMLMSR.java` | `1D939506AC3D366ADB2B35FDB7BEB29A62A9AD7B2B39D83BDC25ACDF643B4A2A` |
| `GMMethod.java` | `D913893A24758F0BF488142A9A826EB2EF16A8E78D118ABC7CA4A7E6A4D8A0AA` |
| `GMOptions.java` | `C58EFD1B95D3E90833B88D24996C88FC7D6110347CD3C3AB880213437DE5591C` |
| `GuessMarket.java` | `671BB76DB878240F157E68FB2561E7BDF745499AF6B14BAD4ED0F97214B50C80` |
| `ObjectFactory.java` | `A134AB70EED5B76E8B05ECE202FDC6A71E4669177E3E145A0768E42B44E379E1` |

Generated source is schema-owned, kept in Git, and never hand-edited. The Task 6 staging comparison proves that generation run. A future reproducibility claim requires a fresh XJC run against these retained hashes.

The generated source compilation gate used Oracle Java 25.0.4 with `--release 25 -encoding UTF-8 -Xlint:all -Werror` and only the five approved JAXB runtime JARs. It compiled all eight sources cleanly with exit code `0` and no warnings or errors. The initial sandbox denial while creating a temporary output directory was rerun through elevated execution and is retained as diagnostic evidence in the Task 6 report. Task 6 adds no mapper or loader behavior; Task 7 proves conversion and Task 8 proves trusted-schema loading.

## Stage 3 Task 7: JAXB business mapping into a quarantined candidate

`JaxbMarketMapper` is a final package-private XML integration helper. Its package-private `map(GuessMarket)` method receives an already XSD-validated generated graph and returns a new `LinkedHashMap<Integer, MarketEvent>`. It creates no JAXB context, opens no file or schema resource, stores no candidate reference, and cannot reach or mutate live Engine state. The later loader will own file, trusted-schema, and JAXB mechanics; the later Engine coordinator will own the single live-map replacement.

`map` retains XML event order by inserting each reconstructed `MarketEvent` into the fresh `LinkedHashMap` during source-order traversal. It accepts every unique Java `int` ID, including zero, negatives, and both endpoints. It joins the generated `xs:list` name tokens with exactly one space and trims the result. It trims only outer whitespace from description and option labels, retaining internal whitespace and case. Present empty or whitespace-only text and duplicate option labels remain valid. The mapper preserves two XML option positions and constructs each new event through its public constructor, which establishes zero quantities, zero balance and collected commission, `OPEN` status, no winner, and empty purchase history.

Business validation is intentionally narrow and produces `XML_DATA_INVALID` only after trusted JAXB structure exists. A duplicate ID reports the one-based XML event number, parsed ID, and `id` field. A commission below `0` or above `90` reports `comision`; a one-option collection reports `GM-options`; and `b <= 0` reports `b`, always with the same event and ID context. The `commissionMode` helper maps the schema's exact `on-purchase` and `on-close` values to DTO enum values. The `invalidData` helper centralizes the checked exception's stable code, corrective recovery hint, and XML context. Missing required elements, malformed XML, or a non-schema commission type remain structural failures for `XmlMarketLoader`, not mapper responsibilities.

`JaxbMarketMapperTest` unmarshals the already-structural supplied and custom fixtures only to feed the mapper's generated input boundary. It proves XML order and the complete mapped state: ID, normalized name and description, two ordered labels, commission mode and percentage, liquidity parameter, zero quantities, zero account and commission values, empty history, `OPEN` status, and absent winner. The test-only `guessmarket.engine.MarketEventTestProbe` calls existing package-private observations and returns a public DTO projection plus `b` to the XML-package test. It does not alter production visibility or reflect on private fields. The focused Java 25 compile and mapper command used `javac --release 25 -encoding UTF-8 -Xlint:all -Werror` with the JUnit console JAR and five approved JAXB runtime JARs, followed by `java -jar tools\\testing\\junit-platform-console-standalone-6.1.1.jar execute --select-class guessmarket.engine.xml.JaxbMarketMapperTest`. The result was 9 tests found, started, and successful, with zero failures, skips, aborts, warnings, or errors. The regression command selected `DtoContractTest`, `GuessMarketEngineUseCaseTest`, `LmsrCalculatorTest`, `MarketEventTest`, and `JaxbMarketMapperTest` after the same strict compile; it reported 57 tests found, started, and successful with zero failures, skips, or aborts. The test excludes `invalid missing description.xml` because that fixture is schema-invalid and belongs to Task 8's loader tests.

## Stage 3 Task 8: trusted-schema file loading and error translation

`XmlMarketLoader` is the public XML file boundary. Its only public operation, `load(Path)`, returns a fresh mapper-produced `LinkedHashMap<Integer, MarketEvent>` or throws the checked `EngineOperationException`. It never reaches live Engine state. The method first rejects a null path and a filename without a case-insensitive `.xml` suffix. It then reads `BasicFileAttributes` once: `NoSuchFileException` becomes `XML_FILE_NOT_FOUND`, any other checked metadata-read I/O failure becomes `XML_FILE_ACCESS_FAILED`, and a confirmed nonregular path becomes `INVALID_XML_PATH`. A known unreadable file, stream-open failure, or genuine input I/O failure while reading also becomes `XML_FILE_ACCESS_FAILED`. A JAXB-wrapped `CharConversionException` is a parser decoding failure, so invalid UTF-8 remains `XML_STRUCTURE_INVALID` rather than being mistaken for path access failure.

`trustedSchema` opens only the packaged classpath resource `guessmarket/engine/xml/GM-EX1-Schema.xsd` with try-with-resources, compiles it as the schema, and disallows external schema and DTD resolution while compiling it. Missing, unreadable, or broken trusted-resource and JAXB setup failures become `ENGINE_CONFIGURATION_ERROR`. `trustedUnmarshaller` installs that schema on a fresh JAXB unmarshaller. The input XML's `xsi:noNamespaceSchemaLocation` is not used to select validation rules. JAXB malformed-document and schema violations, including missing schema-required structure, become `XML_STRUCTURE_INVALID`; when JAXB provides a parser location, the loader keeps one-based line and column context plus the safe diagnostic cause.

After successful structure validation, `load` invokes the package-private mapper boundary. Mapper-owned business failures remain unchanged as `XML_DATA_INVALID`, including their event number, actual event ID, and field context. The loader catches only checked expected I/O and JAXB/schema failures. It does not catch `RuntimeException` or `Exception`, so programming defects stay visible. Three package-private functional seams exist only for deterministic same-package tests: the default production path remains `Files::newInputStream`, `JaxbMarketMapper::map`, and the class-loader trusted-schema opener. The tests can make the real loader path receive an expected input I/O error, mapper defect, missing or unreadable trusted schema, or malformed trusted schema without reflection or a public test API.

`XmlMarketLoaderTest` has 17 active tests. It covers null, a directory explicitly named `folder.xml`, bad suffix, uppercase `.XML`, a path containing spaces, missing files, a misleading XML schema-location hint, malformed XML, invalid UTF-8, missing schema structure, missing, unreadable, and malformed trusted schemas, mapper-owned business context, deterministic expected stream-open I/O translation, visible mapper defects, and input closure after both success and failure. The closure tests move and delete the input after `load` returns or throws, which is observable on Windows. Windows does not provide a stable portable way for these tests to make a normal temporary file unreadable to its owner, so no skipped or disabled permission test was added. Instead, the deterministic stream-open `IOException` executes the public `load(Path)` path through the same try-with-resources and error translation code that a genuine access failure uses.

The review-fix RED gate selected only `translatesInvalidUtf8AsStructuralFailure` and failed with the expected mismatch: `XML_FILE_ACCESS_FAILED` instead of `XML_STRUCTURE_INVALID`. After the narrow production correction, the focused strict gate selected `guessmarket.engine.xml.XmlMarketLoaderTest`: 17 tests found, started, and successful, with zero failures, skips, or aborts. The final strict Java 25 six-suite rerun reported 74 tests found, started, and successful, with zero failures, skips, or aborts.

## Stage 4 Task 9: direct-domain persistence quarantine

`SavedState` is the private versioned serialization root. It contains only format version `1` and a copied ordered `ArrayList<MarketEvent>`. It does not introduce a second snapshot model, replay accepted purchases, or serialize DTO, XML, Engine, calculator, path, or stream objects. The existing direct domain graph retains exact accepted binary64 history and account values, while prices and maximum subsidy remain derived.

`SavedStateStore.resolveStatePath` accepts a base path ending in any case form of `.ser`, otherwise appends `.ser` without disturbing dots or spaces in the filename. It rejects a null path, an existing directory, a target directory, or a missing parent directory as `INVALID_STATE_PATH`. `save` first validates the state in memory, writes one unique sibling temporary file with try-with-resources, attempts `ATOMIC_MOVE` alone, and retries once with `REPLACE_EXISTING` only while the completed temporary file still exists. A fallback success is intentionally non-atomic. A failed move never claims that an earlier disk target was preserved; if the temporary file disappeared after an exceptional atomic attempt, the store reports an access failure without retrying. Best-effort temporary cleanup runs after every unsuccessful publication path.

`restore` first confirms that the resolved target is a readable regular file. It installs a stream-specific object filter with `maxdepth = 32` before reading. The allowlist is limited to the direct saved-state graph, required JDK strings, integers, enum and number superclasses needed by Java serialization, `ArrayList`, and required object or primitive arrays. The store requires exactly one `SavedState` root and rejects corruption, unsupported classes, a wrong root, trailing objects, and incompatible versions as `SAVED_STATE_INVALID`.

`SavedStateValidator.validate` is the quarantine boundary. It returns a fresh insertion-ordered `LinkedHashMap` only after it proves a nonempty exact `ArrayList` event graph, full-range unique event IDs, source-bounded text, two ordered options, positive liquidity, valid commission policy, nonnegative quantities, lifecycle and winner consistency, and distinct mutable event, account, option, collection, and history-record identities. It keeps history in original chronological order for checked quantity accumulation and D-067 account arithmetic. It validates each stored total, commission timing, finite account value, close payout, and total commission with the same eight-ULP binary64 comparison used for stored financial relationships. It deliberately does not recompute LMSR costs or replay purchases.

`GuessMarketEnginePersistenceTest` began as a fourteen-case same-package persistence-boundary test. Those cases cover suffix and path behavior, first save, overwrite, injected atomic and fallback publication paths, failed publication cleanup, missing and corrupt files, wrong and filtered roots, format version, empty and aliased candidates, duplicate IDs, history-quantity and commission-formula inconsistencies reached through serialized state, both `int` ID endpoints, a tiny representable stored history cost, mapper-canonical saved text, and bare relative file paths. The RED command compiled the new test against the pre-Task-9 production source and failed with 23 expected missing-symbol errors for `SavedState`, `SavedStateValidator`, and `SavedStateStore`. A self-review then added a commission-formula RED test, which correctly failed before the narrow validation correction. The Task 9 review fix then reproduced three accepted noncanonical serialized text graphs and a bare-relative-path `NullPointerException`; it added mapper-equivalent text checks and normalizes a returned state target to an absolute path before sibling temporary-file creation. The final focused strict Java 25 Task 9 gate reported 14 successful tests. The seven-suite Task 9 regression gate reported 88 successful tests, zero failures, skips, or aborts, with exactly the five approved JAXB runtime JARs. Task 10 adds five public Engine persistence cases, bringing this class to 19 cases without weakening any Task 9 assertion. The final whole-branch review correction `acf3c36` then added six focused persistence regressions. It checks each raw event, option, and history element's exact class before casting, maps a saved-file disappearance or stream-opening I/O failure to the approved not-found or access code, and rejects every trailing serialized object including `null` as `SAVED_STATE_INVALID`. The current class has 25 active tests.

## Stage 4 Task 10: complete public Engine orchestration

`GuessMarketEngineImpl` is the single public concrete implementation. It owns one private reassignable `Map<Integer, MarketEvent>` initialized as an empty `LinkedHashMap`; emptiness is the only no-loaded-system marker. Its only public constructor is the no-argument production constructor. One package-private constructor accepts the concrete XML loader, saved-state store, and an initial candidate map. A second package-private constructor accepts narrow loader and state-store collaborators so same-package tests can reach otherwise nondeterministic failure families. Both constructors copy the initial map, while successful XML load and restore accept sole ownership of the fully validated candidate returned by their collaborators.

All seven interface methods use the same public checked boundary. `listEvents` creates fresh summaries in load order and returns an immutable list. `getEventDetails` uses the actual full-range event ID and creates a new details snapshot. `purchaseShares` delegates one atomic transition to the event and builds a receipt from its returned record plus details created immediately after that same operation. `closeEvent` returns the final details produced after the atomic close. `saveState` first requires a loaded system and passes only the current ordered domain values to the store. `loadEventsFromXml` and `restoreState` each replace the entire live map reference only after their collaborator returns a complete candidate, so every checked failure preserves the prior system.

The Task 10 RED checkpoint extended the existing public-contract class, added the public XML integration class, and added public persistence cases before production code existed. Strict compilation failed with 12 expected missing-symbol errors, all for `GuessMarketEngineImpl`. The focused GREEN gate compiled with Oracle Java 25.0.4, `--release 25 -encoding UTF-8 -Xlint:all -Werror`, and exactly the five approved JAXB runtime JARs; the three public Engine classes reported 33 successful tests. The original fresh eight-class regression reported 103 tests found, started, and successful with zero failures, skips, disabled tests, or aborts. Source inspection found zero UI imports, `Scanner` imports, or console references in Engine production. `javap -public` confirmed one public implementation constructor and exactly the seven interface operations with only JDK, DTO, and approved Engine contract types.

The Task 10 review-fix RED gate added injected loader and store failures through `GuessMarketEngine` references. After one mechanical unused-import correction, strict compilation failed with seven errors, all for the intentionally absent package-private collaborator seam. The minimal production correction added that seam without changing the public API. One shared package-private `EngineDtoAssertions` helper now compares every details, option, and history field, using `Double.doubleToLongBits` for every binary64 value. The focused strict gate reports 36 successful public Engine tests, including atomic prior-state preservation for `XML_FILE_ACCESS_FAILED`, `ENGINE_CONFIGURATION_ERROR`, and `STATE_FILE_ACCESS_FAILED`. The fresh eight-suite regression reports 106 successful tests with zero failures, skips, disabled tests, or aborts and exactly five JAXB runtime JARs. The substantive review fix is commit `9aaafad`. The final Task 9 correction `acf3c36` expands the complete Stage 4 gate to 112 successful tests; an independent clean rebuild, source-boundary inspection, and final persistence re-review all passed with no remaining finding.

## Stage 5 Task 11: reusable console input and Clean Sections rendering

`ConsoleInput` separates prompt-local text handling from the later command loop. `readMenuChoice` consumes exactly one complete `Scanner` line, returns a valid choice from 1 through 8, and returns `0` after printing the exact parser error for invalid text. It never prints the menu prompt, so the later command loop can render one fresh complete D-072 menu after recovery. The secondary readers trim only outer whitespace and repeat only their own prompts after a blank, non-numeric, overflowing, zero, negative, or out-of-range value. The method names distinguish the menu choice, current displayed event position, purchase option, winning option, positive quantity, and three full-path conversations. The XML, save, and restore readers return `Path` values while preserving spaces inside a supplied filename. A nested checked `EndOfInputException` makes normal input closure different from invalid user text without adding another production UI class. Neither injected stream is closed.

`ConsoleRenderer` centralizes all deterministic presentation, including the sole rendering of the D-072 main-menu prompt. Two named 60-character ASCII constants prevent the menu's equals separator and top-level hyphen separator from drifting. Summary blocks preserve the DTO's load order and XML text while assigning fresh one-based display positions. Detail rendering keeps `OPTIONS`, `ACCOUNT`, and `PURCHASE HISTORY` as subsections, shows the winner only for a closed snapshot, and derives profit, subsidy, or break-even from the unrounded account-balance sign. `formatFinancial` is the one presentation-only money formatter: it uses `Locale.US`, preserves two decimal places, normalizes rendered negative zero, and rejects non-finite values as programming defects.

The Task 11 RED compile used the two new test classes before either production type existed. It failed with 23 expected missing-symbol errors for `ConsoleInput` and `ConsoleRenderer`. The final focused gate compiled the DTOs, required Engine exception types, Task 11 UI source, and the two test classes with Oracle Java 25.0.4 using `--release 25 -encoding UTF-8 -Xlint:all -Werror`. `ConsoleInputTest` and `ConsoleRendererTest` then reported 15 successful tests with zero failures, skips, or aborts. The tests cover one-shot menu recovery with renderer-owned prompt output, exact revised secondary prompts and parsing messages, retry scope, path spaces, end-of-input, the complete menu literal, Clean Sections headings and separators, full and filtered summaries, long text preservation, both commission modes, closed-result categories, newest-first history, every Engine error code with present and absent context, omission of raw exception text, ASCII-only output, and financial formatting.

## Stage 5 Task 12: eight-command console application and startup wiring

`GuessMarketConsoleApp.run` owns one stable loop and one numeric `switch`. The initial menu has no leading blank line. Each invalid choice, completed command, or checked Engine rejection returns through `ConsoleRenderer.renderBlankLine` before one fresh complete menu. This small renderer method was added as an authorized Task 12 integration correction because the accepted Task 11 boundary had no way to express D-072's exact menu-return spacing without leaking output ownership into the application. End-of-input is caught only at the outer input boundary and renders `Input closed. Exiting.`. Each command catches only `EngineOperationException`; unexpected runtime defects and errors remain visible.

The handlers follow the public Engine interface exactly. Details use the complete summary list and translate a fresh one-based position to its actual full-range event ID. Purchase and close independently filter `OPEN` summaries in load order, number that filtered collection from one, translate the chosen position, and let the Engine repeat lifecycle validation. Purchase renders the `PurchaseReceipt` result and its nested same-operation details without a post-purchase query. Close renders the `MarketEventDetails` returned by `closeEvent` without a post-close query. A loaded system with no open events returns through the approved valid-state messages without prompting or calling a mutation. Load, save, restore, explicit exit, and checked recovery remain distinct conversations.

`ConsoleMain` is the only public UI production class and the only `main` entry point. It constructs `GuessMarketEngineImpl` once into a `GuessMarketEngine` variable, creates UTF-8 `System.in` and `System.out` wrappers, composes `ConsoleInput`, `ConsoleRenderer`, and `GuessMarketConsoleApp`, and calls `run`. It does not close either standard stream. The UI production package contains exactly the four approved classes.

Task 12 began with `FakeGuessMarketEngine` and `GuessMarketConsoleAppTest` before either application production class existed. After two mechanical test-command corrections, the valid strict RED compile failed with six missing-symbol errors, all for `GuessMarketConsoleApp` and `ConsoleMain`. A separate focused RED compile for the authorized renderer integration failed with one missing-symbol error for `renderBlankLine`. The focused GREEN gate then reported 25 successful UI tests: 6 input, 10 renderer, and 9 application tests. The exact all-eleven-class Oracle Java 25.0.4 gate used `--release 25 -encoding UTF-8 -Xlint:all -Werror` and exactly the five approved JAXB runtime JARs; all 137 tests were found, started, and successful with zero failures, skips, disabled tests, or aborts.

The temporary classpath smoke conversation used the supplied `multiple.xml`, first exercised pre-load checked recovery, then ran load, list, details, purchase, close, save, restore, and exit. The process exited `0` with empty standard error. Its 11,784-character transcript contained nine complete Clean Sections menus, exactly one blank line before every return menu, all six top-level output titles, the complete long supplied description, exact `Hell Yea !` and `No way !` labels, every success sentence, the two-line recovery meaning, and no ANSI, clear-screen, non-ASCII decoration, wrapping, or truncation. This is development classpath evidence, not the later packaged-process proof.

## Supported Engine methods

| Method | Responsibility | Success result | Expected failure boundary | Status |
| --- | --- | --- | --- | --- |
| `loadEventsFromXml(Path)` | Build a complete trusted XML candidate and replace live state once | Loaded event count | Structured XML or configuration code, prior state preserved | Implemented |
| `listEvents()` | Return compact immutable summaries in load order | Immutable summary list | `NO_SYSTEM_LOADED` | Implemented |
| `getEventDetails(int)` | Return one full immutable event snapshot by actual ID | `MarketEventDetails` | `NO_SYSTEM_LOADED` or `EVENT_NOT_FOUND` | Implemented |
| `purchaseShares(int, int, int)` | Complete one atomic LMSR purchase | `PurchaseReceipt` with same-operation details | Event, option, quantity, lifecycle, or numerical code, state preserved | Implemented |
| `closeEvent(int, int)` | Complete one atomic winner and payout transition | Final `MarketEventDetails` | Event, option, or lifecycle code, state preserved | Implemented |
| `saveState(Path)` | Publish one complete versioned `.ser` file | Normal return | State-path or access code, running state unchanged | Implemented |
| `restoreState(Path)` | Validate a direct-domain saved graph and replace live state once | Restored event count | State-path, access, or invalid-state code, prior live state preserved | Implemented |

## Method ledger

Add constructors, public methods, and meaningful package-private helpers as code is implemented. Trivial getters for one immutable class may share one row when they have the same responsibility.

| Stage | File or type | Method or method group | Responsibility and state effect | Primary proof | Status |
| --- | --- | --- | --- | --- | --- |
| 1 | `CommissionMode` and `EventStatus` | Enum values | Represent the two commission timings and two lifecycle states without display strings | `DtoContractTest` | Implemented |
| 1 | `MarketEventSummary` | Constructor and getters | Preserve a compact event projection, full-range ID, empty text, and exactly two ordered immutable labels | `DtoContractTest.summaryPreservesFullRangeEventIdAndEmptyText` and `summaryDefensivelyCopiesTwoOrderedLabels` | Implemented |
| 1 | `MarketOptionSnapshot` | Constructor and getters | Preserve one numbered option, quantity, and full-precision finite probability | `DtoContractTest.optionSnapshotRejectsInvalidOptionNumberQuantityAndPrice` | Implemented |
| 1 | `TradeHistoryEntry` | Constructor and getters | Preserve one successful purchase's option, quantity, and exact financial breakdown | `DtoContractTest.historyEntryPreservesFinancialBreakdown` | Implemented |
| 1 | `MarketEventDetails` | Constructor and getters | Preserve a self-contained event snapshot with ordered options, newest-first history, account values, and explicit winner absence | `DtoContractTest.detailsDefensivelyCopiesOptionsAndNewestFirstHistory` and `detailsRequiresWinnerOnlyForClosedEvent` | Implemented |
| 1 | `PurchaseReceipt` | Constructor and getters | Preserve the successful purchase breakdown and same-operation resulting details | `DtoContractTest.purchaseReceiptKeepsSameOperationDetails` | Implemented |
| 1 | DTO collection getters | `getOptionLabels`, `getOptions`, and `getPurchaseHistory` | Return stored immutable copies without exposing caller-owned lists | `DtoContractTest.allReturnedCollectionsRejectMutation` | Implemented |
| 1 | `GuessMarketEngine` | Seven supported operations | Define the complete caller capability boundary using only JDK paths, DTO results, and one checked failure type | `GuessMarketEngineUseCaseTest.engineInterfaceDeclaresSevenCheckedOperations` and `javap -public` | Implemented |
| 1 | `EngineErrorCode` | Sixteen enum values | Identify XML, market, calculation, and saved-state failure categories without embedding UI text | `GuessMarketEngineUseCaseTest.errorCodeContainsExactlyApprovedValues` and `javap -public` | Implemented |
| 1 | `EngineOperationException` | Simple and complete constructors | Preserve required failure data, optional context, and a diagnostic cause in one checked type | `GuessMarketEngineUseCaseTest.simpleExceptionPreservesRequiredFields` and `completeExceptionPreservesOptionalContextAndCause` | Implemented |
| 1 | `EngineOperationException` | Structured getters | Return typed `Optional`, `OptionalInt`, enum, detail, and recovery values so callers never parse a message | `GuessMarketEngineUseCaseTest` and `javap -public` | Implemented |
| 2 | `LmsrCalculator` | `totalCost` | Evaluate stable LMSR cost through log-sum-exp without state effects | `LmsrCalculatorTest.maximumSubsidyEqualsInitialTotalCost`, `approvedWorkedExampleMatchesAtFullPrecision`, and `veryLargeQuantitiesRemainFinite` | Implemented |
| 2 | `LmsrCalculator` | `priceForOption` | Derive an observational price through stable two-branch softmax, allowing honest binary64 endpoint rounding | `LmsrCalculatorTest.initialPricesAreOneHalf`, `complementaryPricesSumToOneForUnequalQuantities`, and boundary tests | Implemented |
| 2 | `LmsrCalculator` | `purchaseCost` | Calculate the D-068 cancellation-safe direct delta, combine every negative-difference numerator in `long`, preserve representable tiny values, and reject a required-positive zero | `LmsrCalculatorTest.approvedWorkedExampleMatchesAtFullPrecision`, `suppliedSimulatorOracleCasesMatch`, `largeOpposingTermsUseCombinedLongNumerator`, `moderateHNegativeDifferenceUsesCombinedLongNumerator`, and direct-delta boundary tests | Implemented |
| 2 | `LmsrCalculator` | `maximumSubsidy` | Derive `b * ln(2)` without treating it as account cash | `LmsrCalculatorTest.maximumSubsidyEqualsInitialTotalCost` | Implemented |
| 2 | `CommissionPolicy` | Constructor and commission calculations | Own validated timing and 0 through 90 percentage; calculate either purchase or close commission without state mutation | `MarketEventTest.onPurchaseCommissionAtNinetyPercentCreditsCostAndCommission`, `onCloseCommissionAtNinetyPercentIsRetainedExactlyOnce`, and `zeroPercentCommissionWorksInBothModes` | Implemented |
| 2 | `EventAccount` | Constructor, observations, `commitPurchase`, and `commitClose` | Own zero-based balance and collected commission; accept both prevalidated candidate values only at a named transition commit point | Accounting, close, and complete serialized before-after tests in `MarketEventTest` | Implemented |
| 2 | `MarketOption` | Constructor, observations, and `commitPurchase` | Own one fixed number, XML label, and nonnegative quantity; change quantity only from the aggregate's successful purchase commit | `constructorCreatesOpenZeroBasedEventAndPreservesAllowedXmlValues`, `multiplePurchasesAccumulateQuantitiesAccountingAndNewestFirstHistory`, and `quantityOverflowIsRejectedWithoutChangingAnyDomainState` | Implemented |
| 2 | `TradeRecord` | Constructor, observations, and `toHistoryEntry` | Preserve one successful purchase's option identity and finite exact financial breakdown; create a fresh immutable public history DTO | `multiplePurchasesAccumulateQuantitiesAccountingAndNewestFirstHistory` | Implemented |
| 2 | `MarketEvent` | Public constructor | Establish one self-validating open two-option aggregate with distinct owned state and a zero-based account | `constructorCreatesOpenZeroBasedEventAndPreservesAllowedXmlValues` and `constructorRejectsInvalidDomainDefinitions` | Implemented |
| 2 | `MarketEvent` | `purchase` | Validate lifecycle, option, positive quantity, and checked addition; calculate quantity, cost, commission, total, account, both prices, and record; name operation-specific failures; commit quantity, account, and history only after the candidate succeeds | Purchase, commission, history, `reachableLargeOpposingPurchasesUseCorrectedAccountingCost`, `reachableModerateHOpposingPurchasesUseStableAccountingCost`, commission-underflow identity, boundary, numerical rejection, and complete before-after tests in `MarketEventTest` | Implemented |
| 2 | `MarketEvent` | `close` | Validate open lifecycle and winner; calculate payout, commission, net payout, account, and commission total; name operation-specific failures; commit account, winner, and closed status only after the candidate succeeds | Close commission, lifecycle detail, profit, subsidy, break-even, and complete before-after tests in `MarketEventTest` | Implemented |
| 2 | `MarketEvent` | `toSummary`, `toDetails`, and `maximumSubsidy` | Return fresh immutable public projections and derived values without exposing or changing owned domain state; convert history newest-first | `constructorCreatesOpenZeroBasedEventAndPreservesAllowedXmlValues`, `multiplePurchasesAccumulateQuantitiesAccountingAndNewestFirstHistory`, and all accounting tests | Implemented |
| 2 | `MarketEvent` | Package-private state and ownership observations | Let only same-package Engine coordination and persistence validation inspect exact stored values, nodes, collections, lifecycle, and winner identities without widening the public API | `packagePrivateObservationsPreserveOwnedIdentityForLaterValidation` and public `javap` inspection | Implemented |
| 3 | `JaxbMarketMapper` | `map` | Build a fresh XML-ordered `LinkedHashMap` candidate; normalize allowed text; map commission timing; validate only duplicate IDs, exactly two options, commission 0 through 90, and positive `b`; never perform file, schema, JAXB-context, cache, or live-state work | All 9 `JaxbMarketMapperTest` cases, including supplied `error-2.xml` and `error-3.xml` plus D-069 fixtures | Implemented |
| 3 | `JaxbMarketMapper` | `commissionMode` and `invalidData` | Translate the two trusted schema commission names and centralize `XML_DATA_INVALID` failure context with one-based event position, actual ID, and field | Commission-boundary, duplicate-ID, one-option, and nonpositive-`b` mapper tests | Implemented |
| 3 | `XmlMarketLoader` | `load` | Validate the path, create a fresh trusted-schema JAXB unmarshaller, close the XML stream, map the complete candidate, and preserve mapper failures without touching live state | All 17 `XmlMarketLoaderTest` cases | Implemented |
| 3 | `XmlMarketLoader` | `validatePath` and `hasXmlSuffix` | Classify null and case-insensitive suffix, then use one checked attribute read to distinguish missing, confirmed nonregular, metadata-access, and readability conditions into the three approved path and access codes before opening the input | Loader null, `folder.xml` directory, suffix, uppercase, missing, and deterministic I/O tests | Implemented |
| 3 | `XmlMarketLoader` | `trustedSchema` and `trustedUnmarshaller` | Load the Engine-controlled classpath XSD, configure JAXB validation, and report only installation or JAXB setup defects as `ENGINE_CONFIGURATION_ERROR` | Trusted-hint, valid-load, malformed, missing-structure, and missing, unreadable, or broken trusted-schema tests | Implemented |
| 3 | `XmlMarketLoader` | XML failure helpers and `findCause` | Preserve expected I/O causes, classify malformed and schema-invalid documents with safe parser location, and leave runtime defects visible | Loader access, structure, mapper-context, and visible-defect tests | Implemented |
| 4 | `SavedState` | Constructors and observations | Own format version and a copied ordered direct-domain event collection without exposing a public persistence type | `GuessMarketEnginePersistenceTest.savesAndRestoresAValidatedOrderedDomainGraph` and serialized-candidate rejection tests | Implemented |
| 4 | `SavedStateStore` | `resolveStatePath` | Apply the case-insensitive `.ser` rule, preserve dots and spaces, and reject invalid targets or missing parents | `GuessMarketEnginePersistenceTest.resolvesCaseInsensitiveSuffixWithoutChangingPathsWithDotsOrSpaces` and `rejectsInvalidAndMissingStatePathsWithStructuredFailures` | Implemented |
| 4 | `SavedStateStore` | `save` and publication helpers | Validate, serialize to a closed unique sibling temporary file, attempt atomic publication, conditionally use explicit non-atomic replacement, and clean temporary files without false disk-preservation claims | `savesAndRestoresAValidatedOrderedDomainGraph`, `overwritesExistingStateAndFallsBackWhenAtomicPublicationIsUnsupported`, `doesNotRetryPublicationWhenFailedAtomicMoveConsumesTheTemporaryFile`, and `cleansTemporaryStateWhenFallbackPublicationFails` | Implemented |
| 4 | `SavedStateStore` | `restore`, filter, and restore-target helpers | Require a readable regular file, filter one exact graph to depth 32, reject trailing or incompatible data, and leave validation to quarantine | `mapsMissingCorruptWrongRootAndFilteredFilesToSavedStateFailures` and `restoreQuarantinesSerializedVersionAndSemanticViolations` | Implemented |
| 4 | `SavedStateValidator` | `validate` and graph checks | Prove graph identity, source-bounded values, chronology, accounting, commission, payout, lifecycle, and mutable-alias safety before returning a fresh ordered map | `validatorRejectsWrongVersionEmptyDuplicateAndInvalidHistoryCandidates` and `restoreQuarantinesSerializedVersionAndSemanticViolations` | Implemented |
| 4 | `GuessMarketEngineImpl` | Constructors | Compose production XML and persistence collaborators; expose only one public no-argument constructor; copy injected initial state without widening the supported API | `GuessMarketEngineUseCaseTest.implementationHasOnePublicNoArgumentConstructorAndNoOtherPublicConstructor` and public `javap` inspection | Implemented |
| 4 | `GuessMarketEngineImpl` | `loadEventsFromXml` and `restoreState` | Receive a complete ordered candidate, replace the live map reference once, and return its event count; preserve prior state on every collaborator failure | All 4 `GuessMarketEngineXmlLoadTest` cases plus public restore replacement and failure cases in `GuessMarketEnginePersistenceTest` | Implemented |
| 4 | `GuessMarketEngineImpl` | `listEvents` and `getEventDetails` | Require loaded state, resolve actual full-range IDs, and create fresh immutable DTO snapshots without filtering or exposing domain collections | Ordered, full-range, immutable, missing-ID, and no-loaded-state cases in `GuessMarketEngineUseCaseTest` | Implemented |
| 4 | `GuessMarketEngineImpl` | `purchaseShares` and `closeEvent` | Locate one actual event, delegate its atomic transition, and return same-operation receipt or final details while preserving state and typed context on failure | Successful and failed purchase and close cases in `GuessMarketEngineUseCaseTest` plus continued operation after restore | Implemented |
| 4 | `GuessMarketEngineImpl` | `saveState`, `requireLoadedSystem`, and `requireEvent` | Reject state-dependent calls before load, publish only the current ordered domain graph, and centralize checked no-system and missing-ID context without console behavior | No-loaded-state and missing-ID use cases plus public save success and failure cases in `GuessMarketEnginePersistenceTest` | Implemented |
| 5 | `ConsoleInput` | Menu, selection, option, quantity, and path readers | Read full lines; return `0` after one invalid menu choice so the caller can render a fresh menu; repeat only secondary prompt-local shape errors; preserve internal path spaces; and signal end-of-input without closing borrowed streams | `ConsoleInputTest` | Implemented |
| 5 | `ConsoleRenderer` | `formatFinancial` | Format finite values with two US decimal places, normalize signed zero, and reject non-finite input | `ConsoleRendererTest.formatterUsesUsFixedDecimalsNormalizesNegativeZeroAndRejectsNonFiniteValues` | Implemented |
| 5 | `ConsoleRenderer` | Menu, blank-line separator, DTO, receipt, history, and error renderers | Produce the deterministic D-072 Clean Sections menu, exact return spacing, named ASCII blocks, financial categories, and safe error mappings without domain logic | `ConsoleRendererTest` | Implemented |
| 5 | `GuessMarketConsoleApp` | `run` and command handlers | Own the stable loop, command conversations, OPEN filtering, actual-ID translation, same-operation result rendering, and checked recovery scope | `GuessMarketConsoleAppTest` | Implemented |
| 5 | `ConsoleMain` | `main` | Construct the concrete Engine once into its interface type, create UTF-8 borrowed standard-stream wrappers, compose the three package-private UI collaborators, and start the app | `GuessMarketConsoleAppTest.consoleMainIsThePublicStaticVoidEntryPoint`, source inspection, and temporary classpath smoke | Implemented |

## Stage verification log

| Stage | Verified behavior | Focused tests | Regression evidence | Accepted commit | State |
| --- | --- | --- | --- | --- | --- |
| 1 | JUnit foundation, immutable DTO boundary, and complete supported Engine contract implemented | `DtoContractTest`: 8 successful; `GuessMarketEngineUseCaseTest`: 4 successful | Clean Java 25 compile with `-Xlint:all`; 12 tests successful with zero failures, skips, disabled tests, or aborts; JDK-only DTO dependency and public `javap` inspections passed | Implementation commits `219738d`, `2d4e249`, `d628b14`, and `0912d68`; merged as `d8d9b0c` | Accepted and merged |
| 2 | Cancellation-safe LMSR plus self-validating event state, accounting, chronological history, DTO conversion, serialization boundary, atomic transitions, whole-branch review corrections, and residual numerical correction | `LmsrCalculatorTest`: 16 successful; `MarketEventTest`: 20 successful | Clean Java 25 compile with `--release 25 -encoding UTF-8 -Xlint:all -Werror`; focused Stage 2 gate: 36 successful; 48 total regressions successful with zero failures, skips, disabled tests, or aborts; dependency, visibility, serialization, source, and diff inspections passed | Task 4 `542768d` and `cf14539`; Task 5 `7502964`, `e0a341a`, and `3daadb1`; final review fix `be0ba85`; merged as `6842dfd` | Accepted and merged |
| 3 | Task 6 JAXB resources and generated source, Task 7 business mapping, and Task 8 trusted-schema loading | `JaxbMarketMapperTest`: 9 successful; `XmlMarketLoaderTest`: 17 successful | Fresh Java 25 strict compile with `-Xlint:all -Werror`; all six current suites used exactly five JAXB runtime JARs; 74 tests successful with zero failures, skips, disabled tests, or aborts | Task 6 `43c67d4`; Task 7 mapper `a642d13` and proof `32c6a76`; Task 8 `07d3d6a`, access fix `71ea0f8`, count fix `a1b4d9b`, and review fix `b9e0b4e`; merged as `15c8c8f` | Accepted and merged |
| 4 | Task 9 direct-domain persistence plus Task 10 complete Engine orchestration, immutable public snapshots, actual-ID behavior, XML and restore replacement, restart round trip, continued operations, and failure atomicity | Task 10 public Engine gate: `GuessMarketEngineUseCaseTest` 11, `GuessMarketEngineXmlLoadTest` 6, and `GuessMarketEnginePersistenceTest` 25 successful | Fresh Java 25 strict compile with `--release 25 -encoding UTF-8 -Xlint:all -Werror`; all eight exact DTO and Engine classes used exactly five JAXB runtime JARs; 112 tests successful with zero failures, skips, disabled tests, or aborts; source and public `javap` boundary inspections passed | Task 9 `5eb202b`, `6b722ac`, and final review fix `acf3c36`; Task 10 RED `706df9a`; implementation `f536bdb`; review fix `9aaafad` | Implemented and verified, awaiting Nitzan review |
| 5 | Task 11 input and Clean Sections rendering plus Task 12 complete eight-command application, OPEN filtering, actual-ID translation, same-operation result use, checked recovery, graceful input closure, exact menu-return spacing, and UTF-8 startup wiring | `ConsoleInputTest`: 6 successful; `ConsoleRendererTest`: 10 successful; `GuessMarketConsoleAppTest`: 9 successful | Oracle Java 25.0.4 strict all-eleven-class gate with exactly five JAXB runtime JARs: 137 successful with zero failures, skips, disabled tests, or aborts; temporary supplied-`multiple.xml` classpath smoke exited 0 with empty stderr and passed transcript inspection | Task 11 `681fd47` and `52ccd59`; Task 12 `feat: implement console application flows` | Implemented and verified |
| 6 | Authoritative build and exact ZIP | None before execution | None before execution | None before approval | Planned |
| 7 | Exact-artifact acceptance | None before execution | None before execution | None before approval | Planned |

## Concepts to explain as they appear

- Immutable DTOs and defensive copying.
- Interface reference versus concrete construction.
- Checked exceptions with typed context.
- Encapsulation, aggregate ownership, and meaningful transitions.
- Floating-point cancellation, overflow, underflow, `log1p`, and `expm1`.
- Atomic validate-calculate-commit flow.
- JAXB-generated integration types versus application domain objects.
- Trusted schema loading and layered validation.
- Java serialization, `serialVersionUID`, format version, object filters, and quarantine.
- Dependency injection for deterministic console and file-behavior tests.
- Manifest-relative runtime classpaths and non-fat JARs.
- Exact-artifact verification versus source-level test success.

## Final walkthrough shape

When implementation is complete, this guide must let Nitzan explain the application in this order:

1. How `ConsoleMain` composes the application.
2. How the UI reads one command and calls only the Engine interface.
3. How immutable DTOs cross module boundaries.
4. How XML becomes a fully validated candidate and then live state.
5. How a purchase uses the direct LMSR delta and commits all related state once.
6. How close computes commission and the final market-maker result without double-crediting.
7. How saved state is filtered, validated, and installed without replay.
8. How the build proves tests, JAR ownership, manifest classpath, ZIP membership, and real packaged execution.
9. Which evidence remains manual and why clean Windows 10 cannot be replaced silently by CI or IntelliJ.
